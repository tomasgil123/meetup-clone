import styled from 'styled-components'

const ContainerDatePicker = styled.div`
  text-align: center;
`

const ContainerErrorMessage = styled.div`
  max-width: 350px;
  margin: auto;
`

export { ContainerDatePicker, ContainerErrorMessage }
